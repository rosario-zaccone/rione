
  # Social Network Mockups

  This is a code bundle for Social Network Mockups. The original project is available at https://www.figma.com/design/MJnxQEl57PCVn6XSotFomD/Social-Network-Mockups.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  